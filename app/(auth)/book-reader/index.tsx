import { View, Text } from 'react-native';

const Intro: React.FC = () => {
  return (
    <View className="flex">
      <Text className="text-1">Intro</Text>
    </View>
  );
};

export default Intro;
